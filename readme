1. Установить Redis. В строке 17 файла main.go нужно указать корректный адрес и порт для Redis.

2. В строке 18 файла main.go нужно указать порт, по которому будет доступен сервер.

3. Запустить тесты выполнив команду "go test -v".

4. Запустить программу выполнив команду "go run main.go".

5. Запрос осуществляется к корню, в запросе необходимо передать get-параметры "x" и "y", где "x" - порядковый номер с которого начинается последовательность Фибоначчи, а "y" - порядковый номер с которым заканчивается последовательность Фибоначчи.
Пример запроса: http://localhost:8080/?x=5&y=40.